{
  "name": "kps13",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "next dev",
    "build": "next build",
    "export": "next build && next export -o docs",
    "deploy": "yarn export && echo 'kps13.com' >> docs/CNAME && touch docs/.nojekyll && git add ./docs && git commit -m \"chore(site): regen\" && git push origin main -f",
    "start": "next start"
  },
  "dependencies": {
    "@fullpage/react-fullpage": "^0.1.19",
    "aos": "^3.0.0-beta.6",
    "autoprefixer": "^10.2.5",
    "next": "10.0.8",
    "postcss": "^8.2.8",
    "react": "17.0.1",
    "react-dom": "17.0.1",
    "tailwindcss": "^2.0.3"
  }
}
